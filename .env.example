# QuizForge Environment Variables
# Copy this file to .env.local and fill in your values

# Claude API (required)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Firebase Configuration (required for client-side)
# Get these from: Firebase Console > Project Settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Optional: Analytics
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Firebase Admin SDK (required for server-side auth verification)
# Get these from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# The private key should have newlines escaped as \n
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Stripe Configuration (required for payments)
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_xxxxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Stripe Price IDs (create in Stripe Dashboard)
STRIPE_PRO_PRICE_ID=price_xxxxx
STRIPE_SCHOOL_PRICE_ID=price_xxxxx
STRIPE_UNIVERSITY_PRICE_ID=price_xxxxx

# Sentry (optional - for error tracking)
NEXT_PUBLIC_SENTRY_DSN=

# App URL (for redirect URLs)
NEXT_PUBLIC_APP_URL=https://www.quizforgeapp.com

# =============================================================================
# PRODUCTION RECOMMENDATIONS
# =============================================================================

# Redis Rate Limiting (RECOMMENDED for production)
# The default in-memory rate limiter works per-serverless-instance.
# For reliable rate limiting across all instances, use Redis/Upstash:
# - Free tier available at: https://upstash.com/
# Uncomment and configure:
# UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxxxx

# Firebase Security Rules Deployment
# IMPORTANT: Deploy Firestore security rules to protect your database:
# 1. Install Firebase CLI: npm install -g firebase-tools
# 2. Login: firebase login
# 3. Deploy: firebase deploy --only firestore:rules
