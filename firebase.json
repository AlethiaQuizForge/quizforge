{
  "emulators": {
    "singleProjectMode": true
  },
  "firestore": {
    "database": "(default)",
    "location": "nam5",
    "rules": "git push",
    "indexes": "firebase deploy --only firestore:indexes"
  }
}
